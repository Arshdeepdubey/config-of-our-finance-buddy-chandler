apiVersion: v1
kind: Service
metadata:
  name: crud-service
  namespace: default
  labels:
    app: crud-service
spec:
  type: NodePort
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30080
      protocol: TCP
      name: http
  selector:
    app: crud-service